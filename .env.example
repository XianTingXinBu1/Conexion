HOST=127.0.0.1
PORT=3900
CLIENT_ORIGIN=http://127.0.0.1:3100
DEFAULT_UPSTREAM_BASE_URL=
DEFAULT_UPSTREAM_API_KEY=
# 默认仅在后端绑定本机时允许 127.0.0.1 / localhost / 局域网上游；
# 如果 HOST=0.0.0.0 且确实需要连接本机/内网上游，可显式设为 true。
ALLOW_PRIVATE_UPSTREAMS=
# 请求体最大字节数，默认 1MiB。
MAX_BODY_BYTES=
